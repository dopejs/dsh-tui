/**
 * The package version, shown on the first screen.
 *
 * Inlined at build time rather than read from disk: the published package has
 * no readable manifest relative to the bundled entry point, and a version the
 * UI cannot resolve would render as a lie or a blank.
 */
export const TUI_VERSION = '0.1.0'
